# Git Workflow

- **Свежая база:** перед ветвлением, возобновлением работы, пушем или открытием MR/PR — `git fetch` и rebase на последнюю базу. Никогда не ветвиться/пушить с устаревшего ref. После rebase, который подтянул новые коммиты — перезапустить локальные проверки, релевантные изменениям.
- **Коммиты:** один атомарный коммит на логическую единицу. Большие задачи → один коммит на каждый значимый этап.
- **Сообщения коммитов:** повелительное наклонение, английский язык, ≤72 символа в заголовке. Без type-префиксов (`feat:`, `fix:`). Тело добавлять только когда контекст неочевиден.
- **Именование веток:** `feature/...` (новое продуктовое поведение), `fix/...` (исправление бага), `chore/...` (без изменения продуктового поведения — обновление зависимостей, конфиги, CI, build tooling, форматирование) — kebab-case, английский. Выбирать префикс по *тому, что делает изменение*, а не по размеру.
- **Размещение worktree — внутри проекта:** новые worktree создавать в `<корень репо>/.worktrees/<slug>` (`git worktree add -b <branch> .worktrees/<slug> origin/<base>` из корня), а не соседними каталогами. Разово для проекта убедиться, что каталог игнорируется: строка `.worktrees/` в `.gitignore` проекта; если правка tracked-файлов прямо сейчас нежелательна (чужой diff, командное репо) — `.git/info/exclude` локально и предложить перенести в `.gitignore` отдельным изменением. Предостережения: `git clean -fdx` / `-fdX` в корне удалит и ignored `.worktrees/` — при живых worktree добавлять `-e .worktrees`; не создавать worktree внутри worktree — путь всегда строить от корня основного чекаута. Worktree, создаваемые harness'ом (`EnterWorktree`, `Agent(isolation: worktree)`), выбирают путь сами — правило про явные `git worktree add`.
- **Force push:** обычный `--force` запрещён. `--force-with-lease` / `--force-if-includes` разрешены без подтверждения (lease защищает от затирания чужих коммитов).
- **Git hooks:** никогда не обходить (`--no-verify`, `--no-gpg-sign`, и т.д.) без явной инструкции пользователя. Сбой hook → найти первопричину.
- **Checkpoint перед большими рефакторингами.** Перед тем как дать агенту затронуть несколько файлов, переписать функцию/модуль или выполнить любую многошаговую трансформацию — сначала зафиксировать checkpoint: `git add -A && git commit -m "checkpoint: <что будет меняться>"`. Если агент наделает беспорядка, восстановление — `Esc Esc` в промпте Claude Code (отмена последних правок) или `git reset --hard HEAD` (сброс всего с момента checkpoint). Цель: не более 10 секунд до рабочего состояния.
- **Локальная верификация перед пушем:** пушить только то, что проходит проверки, релевантные изменениям (build changed → build; tests changed → tests; lint config changed → lint; build system changed → release build). Draft-статус не оправдание. Единственная допустимая причина пропустить проверку — явное осознание, что работа неполная.
- **Push feature-ветки без подтверждения:** при работе на выделенной ветке `feature/`, `fix/` или `chore/` (не main/master/develop) — создание ветки, коммиты на каждом этапе, push в remote и открытие draft PR — рутинные операции и подтверждения не требуют. Подтверждение всё ещё нужно для: обычного `--force`, промоции PR (draft → ready for review), слияния в дефолтную ветку. `--force-with-lease` / `--force-if-includes` подтверждения не требуют.
- **Устаревшие gone-ветки:** удалять вручную — `git worktree remove` + `git branch -D` для веток, у которых исчез remote (`gone` в `git branch -vv`); массовая уборка (классификация merged/gone/stale + одно подтверждение) — скилл `worktree-cleanup`; `commit-commands:clean_gone` — вариант, если плагин установлен.

## Репозиторий ~/.claude — PR-only

Полное правило (paths-scoped, грузится только при работе с tracked-файлами `~/.claude`) — [[claude-repo-pr-workflow]]. Коротко: `main` всегда чистая, любая правка tracked-файла (CLAUDE.md, rules/, settings*.json, hooks/, scripts/, skills/, agents/) — через ветку/worktree → PR → auto-merge, никогда напрямую на `main`.

## Подсказки по очистке worktree

**Политика экономии диска (по умолчанию): не держать незанятые worktrees.** Дисковое пространство ограничено (SSD 512 ГБ, Android multi-module worktrees стоят десятки ГБ каждый с кэшами сборки). Как только работа запушена и ready-for-review/смержена, по умолчанию — **удалить** worktree (и локальную ветку — её можно воссоздать с remote за секунды); воссоздавать по требованию когда приходят правки к ревью. «Может понадобится позже» — не причина оставлять. **Когда одноразовость очевидна — чистить автономно, без вопросов:** работа смержена в дефолтную ветку, или всё важное восстановимо с remote, или remote-tracking ветка исчезла. Ветку не жалко, но её ещё нет на remote — сначала запушить (чтобы стала восстановимой), затем удалить локальный worktree+ветку, освобождая диск пользователя. Спрашивать только когда одноразовость реально под вопросом: незафиксированные/незапушенные изменения, которых нет нигде в бэкапе.

При работе в git worktree (не в основном чекауте), в эти моменты — очистить автономно, когда одноразовость очевидна; спросить один раз (не настаивая) только когда она под вопросом:

- **PR/MR смержен, и в worktree нет незафиксированных/незапушенных изменений** → одноразовость очевидна: удалить worktree + локальную ветку **автономно** (`git worktree remove <path>` + `git branch -D <branch>`; при squash-merge ветка формально «не смержена» по SHA — это ожидаемо, её содержимое уже в дефолтной ветке), отчитаться постфактум с путём и именем ветки. Не спрашивать.
- **Ветка запушена, remote-tracking ветка исчезла (`gone` в `git branch -vv`)** → очистить **автономно** `git worktree remove` + `git branch -D` (или скилл `commit-commands:clean_gone`, если плагин установлен). Не спрашивать.
- **Сигнал конца сессии** (пользователь говорит «закончили», «на сегодня всё», «всё, спасибо», завершение в стиле `/exit`, или вы собираетесь объявить многошаговую задачу завершённой) → если worktree этой сессии выглядит законченным (всё запушено/смержено, нет незафиксированных изменений) — очистить **автономно** и упомянуть это в завершающем сообщении. Есть незафиксированные/незапушенные изменения — напомнить, не удалять.

Пропустить подсказку полностью когда:
- Текущий чекаут — основное репо, а не worktree.
- Работа явно в процессе (незафиксированные изменения, незапушенные коммиты, draft PR с активными TODO).
- Пользователь только что создал worktree в этой сессии.

Подтверждение на удаление нужно только когда одноразовость под вопросом — незафиксированные/незапушенные изменения, не восстановимые с remote. Когда работа смержена или восстановима с remote (при необходимости запушив её туда первой) — удалять без вопросов. `git worktree remove --force` / `git branch -D` — не тихие операции: всегда называть путь worktree и ветку в отчёте (постфактум при автономной очистке; заранее — если всё же спрашиваете).
