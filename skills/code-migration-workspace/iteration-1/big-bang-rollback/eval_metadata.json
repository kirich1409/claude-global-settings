{
  "eval_id": 4,
  "eval_name": "big-bang-rollback",
  "prompt": "The legacy analytics module is so tightly coupled with old Firebase Analytics SDK internals that an incremental migration to our new analytics abstraction layer is impossible — everything calls FirebaseAnalytics.getInstance() directly in 40+ places. Let's just do a big bang rewrite on a branch. Start migrating.",
  "assertions": [
    {"id": "requires_rollback_plan", "text": "Agent refuses to start migrating until a rollback plan is agreed with the user"},
    {"id": "asks_rollback_condition", "text": "Agent asks what measurable condition would trigger rollback"},
    {"id": "asks_decision_authority", "text": "Agent asks who has authority to make the rollback decision"},
    {"id": "flags_40_callsites_risk", "text": "The 40+ call sites are flagged as a verification risk specific to big bang (no incremental safety net)"}
  ]
}
